<h1>投稿一覧</h1>

<% @lists.each do |list|%>
  <p>タイトル</p>
  <!--<span><%= list.title %></span>-->
  <span>
    <!-- listにはidが含まれているので「list.id」が可能 -->
    <%= link_to list.title, list_path(list.id) %>
  </span>

<% end %>